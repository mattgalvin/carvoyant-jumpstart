<div class="gray_background">
  <div class="wrapper clearfix">
    <div class="main_content">
      <h3>Check out</h3>
      <p>
        We're still testing out the site and aren't quite ready for orders yet.  Please don't try to check out yet!<br/>
        Your credit card will NOT be charged until your Carvoyant device is ready to ship and you respond to our confirmation email.
        <br />
        <br />
        Enter your email address below.
      </p>
      <%= form_tag "/preorder/prefill", :id => "checkout" do %>
        <%= email_field_tag "email", nil, :placeholder => "hello@carvoyant.com", :required => "required", :id => "email" %>
        <%= hidden_field_tag "preorder", true %>
        <%= hidden_field_tag "quantity", params[:quantity] %>
        <%= submit_tag "Checkout", :class => "blue_button disabled", :id => "amazon_button" %>
      <% end %>
    </div>
    <%= render 'preorder/checkout/sidebar' %>
  </div>
</div>